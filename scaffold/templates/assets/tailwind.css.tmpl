@tailwind base;
@tailwind components;
@tailwind utilities;

.code-block-wrapper {
  position: relative;
}

.code-lang {
  position: absolute;
  top: 0.25rem;
  right: 0.5rem;
  font-size: 0.7rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: #9ca3af;
  pointer-events: none;
  user-select: none;
}
